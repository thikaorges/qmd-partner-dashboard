# MongoDB connection string (Railway provides this with the MongoDB plugin)
MONGO_URL=mongodb://localhost:27017

# Database name
DB_NAME=qmd_partners

# JWT secret (change this in production!)
JWT_SECRET=qmd-hakomed-secret-2026-CHANGE-ME

# Port (Railway sets this automatically)
PORT=8000
